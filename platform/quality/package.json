{
  "name": "idea-receipt-quality",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Platform & quality workstream: unit tests for idea-receipt's pure helpers, error reporter, and widget contracts.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:py": "python3 -m pytest tests -q"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
